/** Host loader entry for the browser notification implementation. */

export type { NotificationSettings, NotificationSound } from '@deepseek-ai/dsh-notifications/types'

/** The separately composed notification owner registers its durable namespace. */
export function apply(): void {}
