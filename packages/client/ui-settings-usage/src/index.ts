/** Host half; usage data is supplied by the dedicated authenticated Remote controller. */
import type { Context } from '@deepseek-ai/cordis'

/** @param _ctx - Host context; presentation registrations belong to the browser half. */
export function apply(_ctx: Context): void {}
