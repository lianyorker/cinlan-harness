/** Opt-in bundle marker for Cinlan Mobile Device composition. @module @deepseek-ai/dsh-cinlan-mobile-device */

/** Cordis plugin name. */
export const name = 'cinlan-mobile-device-bundle'

/** Marker plugin; the package contribution is its exported `cordis.patch.yml`. */
export function apply(): void {}
