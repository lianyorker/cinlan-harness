/** Opt-in bundle marker for Cinlan desktop Computer Use composition. @module @deepseek-ai/dsh-cinlan-computer-use */

/** Cordis plugin name. */
export const name = 'cinlan-computer-use-bundle'

/** Marker plugin; the package contribution is its exported `cordis.patch.yml`. */
export function apply(): void {}
