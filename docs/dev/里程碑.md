Yorker，明白。目标是：

> **以当前 `main` 为唯一架构基线，把 staged-final / integration 中缺失的能力全部集成回来；不重构 main，不替换 main 现有架构，只做增量扩展和必要适配。**

上一版提到的 facade 只能作为兼容实现，不能演变成新的主架构。下面是修正后的完整方案。

# 一、硬性架构约束

以下内容全部保留，不迁移、不替换：

1. **main 当前目录和包分组结构**
2. **当前 `core / session / agent / tools / api / client / bundle` 分层**
3. **main 的分散式 API controllers**
    - `api/session-controller`
    - `api/settings-controller`
    - `api/workspace-controller`
    - `api/workspace-files`
4. **main 的分散式 Client UI 包**
5. **main 的 Session format adjacent migration 链**
6. **main 的 `subagent-claude-code`、`subagent-codex`、`win32-process`、`authorization`**
7. **main 的 `native/system`、`native/desktop-host`**
8. **所有应用继续通过 `dsh` profile 启动**
9. **agent-loop 不直接修改，新增行为通过插件、Service、Tool、Event 接入**
10. **不新增第二套 Agent、Session、Workspace、API controller、Client state owner**

允许新增的内容只包括：

- 新能力 Service Definition / Provider / Consumer / Bundle
- 新的独立包组
- 当前包的插件扩展
- 面向旧 staged-final 契约的兼容入口
- 当前 profile 的可选 patch layer

# 二、来源优先级

`D:\Company\cinlan\cinlan-harness\docs\dev\` 只作为迁移清单和差异分析，实际实现按以下顺序裁决：

```text
当前 main 的代码、类型、测试、架构文档
  >
integration-20260909 的补充实现
  >
staged-final-b84814d3 的缺失能力实现
  >
docs/dev 分析文档
```

具体选择：

- **基础能力**：以 staged-final 为来源，改造成 main 兼容实现。
- **Browser Element Capture / Playwright**：采用 integration 的实现，因为 staged-final 缺失。
- **Session、API、Client、Native、Bundle**：以 main 架构为准。
- **主版本独有功能**：不得因回迁而删除或回退。

# 三、完整迁移范围

## 1. 新增能力包组

### Artifact

```text
packages/artifact/artifact
packages/artifact/artifact-local
```

### Execution Host

```text
packages/execution-host/execution-host
packages/execution-host/execution-host-local
packages/execution-host/execution-host-bind
packages/execution-host/execution-host-ssh
```

### Git

```text
packages/git/git
packages/git/git-local
packages/git/tool-git
```

### Coordination

```text
packages/coordination/coordination
packages/coordination/coordination-local
packages/coordination/coordination-subagent-executor
packages/coordination/tool-coordination
```

### Browser

```text
packages/browser/browser
packages/browser/browser-cinlan
packages/browser/browser-permission-policy
packages/browser/tool-browser
```

integration 补充：

```text
packages/browser/browser-playwright
packages/browser/tool-browser-element-capture
packages/coordination/coordination-browser-element-capture
packages/client/ui-browser-element-capture
```

### Computer Use

```text
packages/computer-use/computer-use
packages/computer-use/computer-use-cinlan
packages/computer-use/computer-use-permission-policy
packages/computer-use/tool-computer-use
```

### Mobile Device

```text
packages/mobile-device/mobile-device
packages/mobile-device/mobile-device-cinlan
packages/mobile-device/mobile-device-permission-policy
packages/mobile-device/tool-mobile-device
```

### Security

```text
packages/security/assessment-scope
packages/security/assessment-scope-session
packages/security/assessment-scope-static
packages/security/finding
packages/security/finding-session
packages/security/security-skills
packages/security/security-workflow-prompt
packages/security/tool-finding
packages/security/tool-vuln-kb
packages/security/vuln-kb-nvd
packages/security/vuln-kb-service
```

### Design Studio

```text
packages/design/design-studio
packages/design/design-studio-local
packages/design/design-studio-prompt
packages/design/tool-design-studio
```

### Voice

```text
packages/voice/voice
packages/voice/voice-sherpa-onnx
```

## 2. Client 包

新增：

```text
packages/client/ui-better-sidebar
packages/client/ui-brand-cinlan
packages/client/ui-design-studio
packages/client/ui-voice-dictation
packages/client/ui-settings-cinlan-capabilities
```

`client/runtime` 的处理方式：

- 可以恢复包入口和必要导出；
- 只能作为当前分散式 Client 的兼容组合层；
- 不允许成为新的全局状态中心；
- 不允许替换 main 的 `client/*` 包。

## 3. API 与 TUI

新增：

```text
packages/api/codex-app-server
packages/api/codex-app-server-harness
packages/interaction/tui
packages/interaction/codex-tui
```

约束：

- `codex-app-server` 只能调用 main 现有 controllers 和 Service；
- 不复制一套新的 Session、Settings、Workspace 实现；
- `host/apiproxy` 如果保留，必须是 RPC 适配模块，不能成为第二套 API 入口；
- TUI 作为新增交互 Consumer，不修改 Agent Loop。

## 4. 共享包

```text
packages/guard/turn-budget-policy
packages/preset/agent-execution-guidance
packages/session/session-persistence-sqlite
packages/subagent/tool-subagent-report
packages/test-support/acp-snapshot
packages/web/web-permission-policy
```

`code-runtime-python`：

- 保留 main 当前 `experimental/` 位置；
- 只同步协议、类型和测试；
- 不为了目录与 staged-final 一致而移动包。

## 5. Bundle

新增并保持可选：

```text
packages/bundle/cinlan-browser
packages/bundle/cinlan-computer-use
packages/bundle/cinlan-mobile-device
packages/bundle/security-findings
packages/bundle/security-skills
packages/bundle/security-workflow
packages/bundle/vuln-kb
packages/bundle/design-studio
packages/bundle/execution-host-ssh
packages/bundle/cinlan-tui
packages/bundle/cinlan-codex-tui
packages/bundle/cinlan-web
```

不得删除 main 现有：

```text
base
web-app
headless
sdk-app
sdk-minimal
acp-app
```

# 四、实施阶段

## Phase 0：基线与契约冻结

只读盘点，不写业务代码。

产出：

1. main / staged-final / integration package manifest 差异表
2. 完整依赖 DAG
3. `ctx` 服务名和注入项表
4. Tool 名称和权限类表
5. SessionEventMap / Projection / Persistence 兼容表
6. Settings slots 和 Client contract 对照表
7. Bundle / profile 装配表
8. Native / 外部依赖清单
9. 需要适配的旧 API 清单

必须冻结：

- Artifact 服务名和导出名
- Browser Element Capture 是否随 `cinlan-browser` 一起启用
- Playwright 是否作为可选 Provider
- `client/runtime` 仅兼容层的边界
- `codex-app-server` 的 RPC 到 main controller 映射
- SQLite 与 main Session Persistence 的物理兼容方式

## Phase 1：基础设施与包接入

先完成不会影响产品默认行为的基础工作：

- `pnpm-workspace.yaml`
- `tsconfig.base.json`
- Host / Client compiler references
- Vitest package discovery
- package manifest 和 workspace dependency
- generated package inventory
- invariant 配置
- README / JSDoc 基础骨架

此阶段不启用新 Bundle，只确保新增包可以独立编译、测试、加载和卸载。

## Phase 2：Execution Host、Artifact、Git

实现顺序：

```text
execution-host Service Definition
  → local Provider
  → bind Consumer
  → SSH Provider

artifact Service Definition
  → local Provider

git Service Definition
  → local Provider
  → tool Consumer
```

关键验收：

- Execution Host lease generation 和 stale lease
- SSH host key pin
- 远程 workspaceRoot 路径限制
- credentialRef 引用
- Artifact 原子写入、SHA-256、授权范围、字节限制
- Git 环境墓碑、只读约束、workspace 绑定
- Provider dispose 和 HMR 注册回收

## Phase 3：Coordination 与设备控制

实现：

- Coordination DAG 调度器
- 子代理 Executor
- Coordination tools
- Browser 基础操作
- Browser 权限策略
- Element Capture
- Playwright Provider
- Computer Use
- Mobile Device
- 对应工具和 Bundle

关键验收：

- DAG 依赖、并发限制、失败传播、取消传播
- 审批请求和超时
- 子代理依赖上下文截断
- Browser 元素选择、身份验证、截图裁剪
- Computer / Mobile 每次 mutation 前必须 observe
- 权限在执行器层拒绝，而不是只依赖 tool schema
- 所有设备 Provider 缺失时显式失败

## Phase 4：Security 完整闭环

实现顺序：

```text
assessment-scope
  → finding
  → finding-session
  → vuln-kb
  → tool-finding / tool-vuln-kb
  → security-workflow-prompt
  → security bundles
```

必须完成：

- 八级 assessment operation 顺序
- 目标、egress、凭据引用和时间窗口
- Finding canonical identity 去重
- `finding/change` Session 事件
- Finding projection、状态查询、分页
- SARIF / Markdown / JSON 导出
- Artifact 报告发布
- NVD / OSV Provider 可替换
- 25 个 security skills 注册
- Workflow Prompt 与 Tool 的一致性

安全模块不得绕过：

- `ctx.credentials`
- `ctx.artifacts`
- `ctx.executionHosts`
- `ctx.sessions`
- `ctx.tools`
- assessment scope executor enforcement

## Phase 5：Design Studio 与 Voice

### Design Studio

完成：

- 文档 create/read/update/delete/list
- 版本递增
- HTML / SVG / Markdown 限制
- preview
- export
- `DESIGN.md` 读取
- system prompt 和 tools
- Design Studio Bundle
- `ui-design-studio`

### Voice

完成：

- `ctx.voice`
- sherpa-onnx Provider
- 模型 registry / cache
- Range 下载、重试、并发、超时
- `/voice/api`
- native addon 状态
- trust fence
- Voice UI 和 Settings 集成

Voice 缺少 native addon 时必须返回明确状态和修复信息，不允许静默启用半可用功能。

## Phase 6：Settings 与现有 Client 集成 ✅

目标：将 staged-final 的全页面 Settings 布局同步到 main，替换原有模态弹窗布局。

### 已完成

- 全页面布局（`region` 语义，非 `dialog`），独立内容滚动
- 搜索栏：实时过滤 section 标签，空结果本地化提示
- `settings.section.icon` keyed slot：每个 section 可注册导航图标
- 返回按钮（`Back to app`）替代模态关闭按钮
- Escape 关闭页面，焦点落在返回按钮
- `settings.section.icon` 默认注册 `IconSettingsOutline16`（general）
- `shell-contract.ts`：移除 `connectionState` / `reconnect`，新增 `settings.section.icon`
- `locales.ts`：新增搜索和 `openDocument` 本地化键，移除 `connection.*` 键
- `package.json`：移除 `@deepseek-ai/dsh-client-connection` 依赖注入
- 全部 6 个测试文件更新，46/46 通过

### 待迁移（后续 Phase）

- Cinlan 能力页面（`ui-settings-cinlan-capabilities`）
- `ready/loading/attention/missing` 状态
- 能力组件数量和刷新机制

### 约束

新增 Client 包必须接入 main 现有：

- `client/store`
- `client/resources`
- `client/modules`
- `client/ui-slots`
- `client/ui-settings`
- `client/ui-settings-general`

不能引入第二套全局状态管理和路由系统。

## Phase 7：Codex App Server 与 TUI

### Codex App Server

按以下方式集成：

```text
RPC schema
  → rpc-map
  → main controller / Service
  → response normalization
```

保留 main 作为真实实现：

- Session controller
- Settings controller
- Workspace controller
- Workspace files
- Subagent、Goals、Jobs、Skills、Approvals 等现有 Service

增加：

- RPC schema
- Fetch handler
- InProcess client
- Codex App Server harness
- normalize
- session export

### TUI

TUI 只作为新的 Interaction 层：

- 复用 main Agent / Session / Tools / Approval / Settings
- 不修改 Agent Loop
- 不创建第二套会话持久化
- 不绕过 `dsh` 启动约束
- 使用现有 `@earendil-works/pi-tui` patch

## Phase 8：共享能力与测试支持

加入：

- `turn-budget-policy`
- `agent-execution-guidance`
- `session-persistence-sqlite`
- `tool-subagent-report`
- `acp-snapshot`
- `web-permission-policy`
- Python runtime 协议对齐
- Examples

SQLite 必须适配 main 当前：

```text
SessionPersistence
Session format migration chain
Session projection
JSONL logical events
```

不能将 staged-final 的物理 schema 假设直接覆盖 main 的 Session 版本机制。

## Phase 9：Bundle、Profile 和默认组合

每个能力完成后按以下方式接入：

1. 先独立 package 测试；
2. 再创建 Bundle；
3. 使用 `dsh --profile ... --dump-config` 检查；
4. Loader 真实加载；
5. 运行 unload / dispose；
6. built artifact smoke；
7. 最后才加入可选 profile；
8. 默认 profile 最后评估是否启用。

默认 profile 不在中途切换架构，只增加可控 patch rows。

## Phase 10：文档和发布收口

更新：

- 包 README / README.zh.md
- JSDoc
- `docs/architecture*`
- Capability seam 文档
- 10 组能力文档
- Settings / Client / TUI / API 文档
- Security 文档
- `.i18n.yaml`
- generated catalogs
- Agent Notes
- snapshots
- package inventory
- workspace / lockfile

用户可见 UI 变更必须补：

- 真实服务器流程验证
- UI snapshot
- GIF 证据

# 五、分阶段验收

每个阶段只运行与当前变更匹配的检查：

```text
pnpm run typecheck
pnpm run build
pnpm run lint
pnpm run hygiene
pnpm run duplication
pnpm run test:snapshot
pnpm run test:docs
pnpm run doc-sync
```

最终收口再运行：

```text
pnpm run test
pnpm run test:coverage
pnpm run test:e2e
```

必须额外验证：

- main 现有 profile 仍能启动；
- main 独有包未被删除或替换；
- Session migration 链未被破坏；
- 新增 Service 可卸载；
- 所有 model-visible 输入可从 Session log 重建；
- 新增 RPC 不绕过 controller；
- 新增 Client 不建立第二套 state owner；
- 新 Bundle 不产生新的应用入口；
- Windows / Linux / macOS 的 Provider 行为分别验证。

# 六、最终交付结构

建议拆成以下独立提交，但全部基于同一个 main 架构：

```text
1. baseline / contract inventory
2. workspace / tsconfig / test scaffolding
3. execution-host + artifact + git
4. coordination + browser/device
5. security stack
6. design + voice
7. settings + client additions
8. codex app server + TUI
9. shared packages + SQLite + examples
10. bundles + profiles
11. documentation + snapshots + final gates
```

最终判断标准不是“是否把 staged-final 目录原样搬回”，而是：

> **main 的架构、入口、Session、API、Client、Native 和独有功能保持不变，同时 staged-final/integration 的能力在 main 上完整可用。**

本轮仍未修改文件。下一步应先执行 **Phase 0 只读盘点**，确认包级契约和适配点后，再开始第一批基础包集成。
